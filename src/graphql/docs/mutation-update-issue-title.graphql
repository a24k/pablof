mutation updateIssueTitle($issue: ID!, $title: String!) {
  updateIssue(input: { id: $issue, title: $title }) {
    issue {
      id number title body state
      milestone {
        id number title description state dueOn
      }
    }
  }
}
