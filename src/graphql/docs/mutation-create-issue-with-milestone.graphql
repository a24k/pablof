mutation createIssueWithMilestone($repository: ID!, $title: String!, $milestone: ID!) {
  createIssue(input: { title: $title, repositoryId: $repository, milestoneId: $milestone }) {
    issue {
      id number title state
      milestone {
        id number title description state dueOn
      }
    }
  }
}
